from django.apps import AppConfig


class ActivityConfig(AppConfig):
    """App configuration for notifications and status updates."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "activity"

